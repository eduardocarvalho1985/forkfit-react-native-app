
// React Native Firebase configuration
import { firebase } from '@react-native-firebase/app';
import '@react-native-firebase/auth';
import '@react-native-firebase/firestore';

// Firebase config is read automatically from GoogleService-Info.plist (iOS) and google-services.json (Android)
// No need to manually initialize with config object in React Native Firebase

export default firebase;
